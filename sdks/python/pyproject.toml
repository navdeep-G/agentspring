[project]
name = "agentspring-sdk"
version = "1.0.0"
description = "Python client for AgentSpring API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["httpx>=0.27.0"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
